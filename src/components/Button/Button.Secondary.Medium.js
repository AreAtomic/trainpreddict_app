const ButtonSecondaryMedium = (props) => {
    return (
        <div {...props} className="button is-light is-medium">
            {props.nom}
        </div>
    )
}

export default ButtonSecondaryMedium
